public class Merge
{
    public static void main(String[] args) {
        StdOut.println("line0");
        // int N = args.length;
        // StdOut.println(N);
        // String delim = args[0];
        // In[] inputs = new In[N-1];
        // StdOut.println("line1");
        // for (int i = 1; i<args.length; i++) {
        //     StdOut.println("line" + i);
        //     inputs[i-1] = new In(args[i]);
        // }
        // StdOut.println("line");
        // while (!inputs[0].isEmpty()) {
        //     StringBuilder sb = new StringBuilder();
        //     for (int i = 0; i<inputs.length; i++) {
        //         sb.append(inputs[i].readLine());
        //         sb.append(delim);
        //     }
        //     StdOut.println(sb.substring(0, sb.length() - 1));
        // }
    }
}
